package org.example.language_fundamentals;

public class CommandLineArguments {

    /*
      Arguments Passing from command line to main method
      Arguments passed from command prompt >> passed as an array of strings to the main method.
      java test A B C D  // args[0] = A // args.length = 4
      Arguments >> String form
      WHY STRING[] args? >> most common used + can be converted to any type.
    
    
    */
}
